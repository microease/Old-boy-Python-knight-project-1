# server端
# import socket
# 实例化一个对象 sk = socket.socket()
# sk.bind(('ip地址',端口号))
# sk.listen(数字)

# conn,addr = sk.accept()    # 阻塞
# conn.send(bytes类型的内容)
# msg = conn.recv(数字)
# ...打印 操作
# conn.closs()

# sk.close()