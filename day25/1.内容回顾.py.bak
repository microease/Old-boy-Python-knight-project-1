# collections
# 数据类型的扩展
# 有序字典 ： 3.6之前 ，Ordereddict
# 默认字典 ： defaultdict(callable)
    # defaultdict(dict)/defaultdict(list)
    # defaultdict(str)/defaultdict(int)
    # defaultdict(lambda : 默认值)
# 双端队列 ： deque
    # 从中间增删数据效率高
# 可命名元组：是一个只有属性没有方法的类，并且属性不能修改
    # 不能通过索引取值，而是通过属性的方式取值
# 计数器

