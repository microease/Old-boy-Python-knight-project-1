01 昨日内容回顾
    1，函数名的应用：
        函数名可以作为变量。
        函数名可以作为函数的参数。
        函数名可以作为函数的返回值。
        函数名可以作为容器类数据类型的元素。
    2，闭包：
        内层函数对外层函数的变量（非全局）的引用，并将函数名返回。
            __closure__
    3,迭代器：
        内部含有__iter__方法的可迭代对象。
            str,list,dict,tuple,set,range
        内部含有__iter__方法,并且含有__next__方法的迭代器。
        l1 = [1,2,3]
        iter1 = l1.__iter__()    iter(l1)
        iter1.__next__()         next(iter1)
        for 循环内部原理。


02 作业讲解
03 生成器
04 列表推导式，生成器表达式
05 作业
06 预习内容


