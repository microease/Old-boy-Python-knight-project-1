# -*- coding: utf-8 -*-
# @Time    : 2018/8/22 9:06
# @Author  : 骑士计划
# @Email   : customer@luffycity.com
# @File    : 02 内容回顾.py
# @Software: PyCharm
# min：返回可迭代对象的最小值（可加key，key为函数名，通过函数的规则，返回最小值）。

# l1 = [(1,1000),(2,18),(3,500)]
# # l2 = [1,4,5,77,88,-1]
# def func(x):
#     return x[1]
# print(min(l1,key=func))

# 1,他会将iterable里面的每个元素自动提取出来。
# 2,他会将你的返回值作为比较大小的具体条件。
# 3，他会将遍历的元素返回回来。
# max：返回可迭代对象的最大值（可加key，key为函数名，通过函数的规则，返回最大值）。